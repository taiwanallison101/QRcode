

/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export', // 這行是關鍵，告訴 Next.js 我們要匯出靜態檔案
  images: {
    unoptimized: true, // 靜態匯出時需要關閉圖片優化
  },
};

export default nextConfig;